#+TITLE: Tycho Gemini-Only Delta Backlog Repair — Luna Handoff
#+DATE: 2026-08-26
#+STARTUP: overview

* Non-negotiable outcome
Every application-facing Tycho Delta, past and future, must be generated by
Gemini and satisfy the grounded semantic schema. Python path/version diffs must
never again be canonical Deltas.

At completion:
- =tycho.deltas= contains only =delta@2= rows generated by
  =gemini-3.7-flash@semantic-differ-1=;
- every historical observation transition that previously had a =delta@1= row
  has one validated Gemini =delta@2= replacement;
- old mechanical rows remain immutable only in a clearly named audit archive,
  never in the canonical table or normal read path;
- production acquisition has no Python-Delta fallback: Gemini failure creates
  no Delta and remains retryable;
- no active claim cites a legacy Delta;
- backfill never publishes to Pub/Sub or silently triggers live claim mutation.

The user explicitly authorized this repair on 2026-08-26. Perform it end to end,
with reversible checkpoints and no deletion of immutable audit data.

* Read first
Read completely, in order:
1. =docs/tycho-spec.org= — update this contract before contradictory code.
2. =docs/architecture.md=.
3. =docs/semantic-delta-deployment-evidence.org=.
4. =docs/agent-runtime-production-evidence.org=.
5. =data/semantic_delta_production.json=.
6. =data/semantic_delta_replay.json= and
   =data/semantic_delta_replay_retry.json= using scripts, not by copying their
   full payloads into chat/logs.

Use Google Vertex/Agent Platform ADC only. Never load the local AI Studio key.

* Current state
- Production acquisition mode: =semantic=.
- BigQuery =tycho.deltas=: 50 rows total, 46 =delta@1= and 4 =delta@2=.
- Observations/raw objects: 92 / 92.
- Firestore: 27 active-or-historical claims, 27 analyst runs, 8 generation runs,
  8 generation leases.
- The four production =delta@2= rows are valid Gemini noise.
- The saved final replay covers 42 of the 46 legacy pairs. It contains 41 valid
  candidates and one deterministic grounding rejection; the valid retry for the
  rejected pair is in =data/semantic_delta_replay_retry.json=.
- Four additional legacy pairs were created during production shadow validation.
  Their semantic shadow outcomes were noise, but model response text was
  deliberately not persisted; regenerate only these four pairs.
- Pub/Sub still routes to the managed Agent Runtime dispatcher.

* Important interpretation
Do not literally delete or overwrite old rows. Preserve the old physical table
as an immutable audit log, but remove it from the canonical name and all normal
application reads.

Target storage:
- =tycho.delta_audit_log_20260826=: immutable original/raw Delta log, including
  legacy rows and any semantic rows used to build the replacement;
- =tycho.deltas=: fresh canonical, insertable, partitioned table containing only
  strict =delta@2= Gemini rows.

An archive table is not a second Delta domain. It is evidence for migration and
rollback. Runtime, analyst, consumers, briefs, Q&A, and normal CloudBackend reads
must use only =tycho.deltas=.

* Hard constraints
1. Update =docs/tycho-spec.org= and =docs/architecture.md= first.
2. Do not rerun Gemini for the 42 saved replay pairs. Revalidate and import the
   saved Gemini outputs deterministically.
3. Make at most four Gemini 3.7 calls for the four shadow-only legacy pairs.
4. Backfill traffic must never publish Pub/Sub, invoke Runtime, create alerts, or
   mutate claims implicitly.
5. Preserve every Observation, raw GCS object, old Delta row, generation record,
   analyst record, and trace.
6. Do not delete the old analyst, disposable probe, Runtime, Registry entry, or
   archive table.
7. Do not change Pub/Sub endpoint, OIDC identity, audience, IAM, or global
   =gcloud= configuration.
8. Pause Scheduler only for the bounded final table/reader cutover; resume it
   after verification.
9. Never create a Python-generated replacement Delta, generic noise reason, fake
   evidence quote, or inferred removal.
10. If Gemini or validation fails, retain a durable retryable pair and create no
    canonical Delta.

* Phase 1 — Change the contract
Make the spec state explicitly:
- =delta@1= is retired from the canonical domain and exists only as immutable
  migration audit evidence;
- all canonical Deltas, including noise, are =delta@2= from Gemini 3.7 Flash;
- acquisition has no mechanical fallback;
- model/provider/validation failure advances nothing and retries later;
- historical Gemini backfill is non-publishing;
- active claims may cite only canonical =delta@2= IDs;
- a one-time governed legacy-claim retirement/supersession migration is allowed
  and must preserve claim history.

Remove documentation that describes =legacy= as a valid production authority or
rollback. Operational rollback is pause/retry/previous semantic image, never
Python Delta generation.

* Phase 2 — Build a resumable backfill importer
Create a dedicated command, for example:
=infra/backfill_semantic_deltas.py=.

It must support:
- =--project=, =--dataset=, =--dry-run=, =--apply=, and =--resume=;
- explicit report/retry-report paths;
- bounded summary output with no model/source payload text;
- a durable local/cloud manifest keyed by
  =(obs_before, obs_after, generated_by, prompt_version)=;
- idempotent lookup by =comparison_id= before insertion;
- crash-safe restart after validation or insert.

** Import the 42 saved Gemini results
For each legacy pair:
1. Locate the old row only for entity/source/observation IDs and chronology.
2. Load both immutable raw snapshots from GCS.
3. Select the saved =semantic-delta@2= model output. For the one failed full-run
   case, use the successful candidate in the retry report.
4. Parse the model output through the current strict Pydantic proposal schema.
5. Re-run current Python grounding, field-boundary, observation-side, enum,
   bounds, duplicate, entity-relevance-policy, version-only, and
   removal-by-absence validation against the real snapshots.
6. Construct a fresh canonical Delta with a new Delta ID, deterministic
   comparison ID, current generator/prompt metadata, and the original transition
   chronology. Record replay provenance in the migration manifest rather than
   pretending a new provider call occurred.
7. Insert without Pub/Sub publication.

Record the saved report SHA-256 and original legacy Delta ID in the manifest.
Import token/cost metadata as historical evidence, clearly marked
=historical_replay_import=, not as a new live call.

** Generate the four missing historical semantic results
Derive the exact four legacy observation pairs absent from the replay by joining
BigQuery rows on =comparison_id=/observation pair, not by hard-coding guessed
IDs.

Run the existing =SemanticDiffer= with Gemini 3.7 Flash once per missing pair.
Add a =historical_backfill= traffic type that:
- uses the normal lease/retry state machine;
- inserts only after full validation;
- never publishes, regardless of meaningful/noise result;
- persists only safe run metadata;
- supports shadow-lease promotion without duplicate calls.

Do not use ordinary =mode=semantic= if that path can publish a meaningful result.

* Phase 3 — Build and validate the complete Gemini set
Initially append the 46 historical =delta@2= replacements to the current raw
=tycho.deltas= table without publication. This keeps every new ID resolvable
while claim hygiene runs.

Expected raw-log state before canonical table swap, subject to direct distinct
pair verification:
- 46 immutable =delta@1= rows;
- 50 Gemini =delta@2= rows: 46 historical replacements plus the 4 already-live
  semantic rows;
- one Gemini =delta@2= per distinct historical/live observation pair under the
  current generator/prompt identity;
- no pair missing and no duplicate comparison ID.

Do not rely only on expected counts. Prove with anti-joins:
- every legacy pair maps to exactly one =delta@2=;
- every =delta@2= references existing before/after Observations of the same
  entity/source;
- every canonical row has =schema_version=delta@2=,
  =diff_kind=semantic=, non-empty =comparison_id=, Gemini =generated_by=, and
  =semantic-delta@2= prompt version;
- meaningful rows have 1–8 grounded changes and scopes;
- noise rows have zero changes/scopes and a non-empty reason.

* Phase 4 — Eliminate Python Delta production paths
Change code and deployment so production cannot write =delta@1=:
- semantic generation is the only cloud acquisition path;
- remove =legacy= and =shadow= from production deploy choices and docs;
- if legacy helpers remain for tests/audit decoding, isolate them behind an
  explicit local/test-only guard that cloud startup rejects;
- change the default from =legacy= to =semantic=;
- remove mechanical triage/publication fallthrough after semantic failure;
- provider or validation failure records a failed generation attempt and returns
  without a Delta;
- =infra/bootstrap.py= creates a strict v2-only canonical table for fresh
  environments;
- normal =CloudBackend.get_delta=/queries reject or cannot see =delta@1=;
- analyst/dispatcher must reject non-v2 IDs for new delivery;
- claim creation/supersession tools must reject new evidence referencing
  noncanonical/legacy Delta IDs.

Keep archive-reading methods separate and named explicitly, for migration/audit
only. Never make an archive query the default.

* Phase 5 — Governed legacy claim hygiene
The 27 claims cannot remain active with =delta@1= evidence.

Add a one-time, resumable migration command, for example:
=infra/migrate_legacy_claims.py=. It must use the legacy-to-v2 pair map and
preserve every claim document/history entry.

Rules:
1. Process only claims with active evidence pointing to archived =delta@1= IDs.
2. If the mapped Gemini =delta@2= is noise, retire the legacy claim through a
   narrowly scoped migration tool. The tool may act only when all old evidence
   maps to validated v2 noise for the same source transition. Record the v2 ID,
   reason, actor, timestamp, and previous state in history.
3. For mapped meaningful v2 rows, run the existing governed analyst in an
   explicit =migration= mode over the canonical semantic Delta. Do not use
   Pub/Sub. New/replacement claims must cite only v2 evidence and obey normal
   fact, inference, scope, confidence, supersession, and demotion rules.
4. A dedicated Gemini curator may decide whether an old claim is superseded by a
   new v2-backed fact or retired as unsupported/release-diary noise. Its tools
   must hard-enforce the pair map and cannot touch nonlegacy claims.
5. Do not rewrite claim statements/evidence in place. Supersede or retire with
   complete history.
6. Suppress external alerts/delivery during migration, but retain bounded audit
   records.
7. Finish with zero active claims containing a =delta@1= evidence ID.

Use Google models/Vertex ADC only. Keep calls bounded to the actual legacy claim
set; do not run an open-ended agent loop.

* Phase 6 — Create the v2-only canonical table
Test BigQuery table-rename support and the full procedure on disposable tables
first.

Production sequence:
1. Record table metadata, schema, partitioning, row counts, hashes/counts by
   schema/entity/source, Pub/Sub backlog, claims, and active job executions.
2. Pause =tycho-nightly= and verify no =tycho-acquire= execution is active.
3. Verify the subscription has no unprocessed migration-related message.
4. Create =tycho.deltas_v2_candidate= with the fresh strict v2-only schema and
   matching partitioning/metadata.
5. Copy only validated =delta@2= rows from the raw table into the candidate.
6. Run all anti-join, uniqueness, schema, and provenance checks again.
7. Rename the current physical =tycho.deltas= table to
   =tycho.delta_audit_log_20260826= without rewriting/deleting rows.
8. Rename =tycho.deltas_v2_candidate= to =tycho.deltas=.
9. Deploy/read back code that writes and reads the new v2-only canonical table.
10. Execute one acquisition at most. If unchanged, accept it; do not force calls.
11. Resume Scheduler and verify its next schedule.

If atomic rename is unsupported, stop and report the exact blocker. Do not delete
or truncate the current table and do not improvise an unsafe copy/delete swap.

* Phase 7 — Required validation
Repository:
- full pytest, Ruff, and compile checks pass;
- fixtures contain no canonical =delta@1= example except clearly named archive
  compatibility fixtures;
- tests prove backfill idempotency, crash resume, failed grounding, no publish,
  four-pair bounded provider calls, canonical-table selection, archive isolation,
  claim retirement/supersession guards, and rollback.

Production canonical invariants:
- =SELECT COUNTIF(schema_version != 'delta@2') FROM tycho.deltas= returns 0;
- =SELECT COUNTIF(generated_by != 'gemini-3.7-flash@semantic-differ-1')= returns 0;
- =SELECT COUNTIF(diff_kind != 'semantic')= returns 0;
- no duplicate =comparison_id=;
- every row joins to both immutable Observations;
- every historical legacy pair has exactly one canonical replacement;
- archived legacy row count and IDs equal the pre-migration inventory;
- zero active claim cites an archived/v1 Delta;
- every active claim evidence ID resolves in canonical =tycho.deltas=;
- backfill produced zero Pub/Sub deliveries, Runtime live runs, or alerts;
- live subscription endpoint/identity/audience are unchanged;
- production acquisition remains Gemini-only after restart/deploy.

Inspect Cloud Logging/Trace for structural metadata only. Search explicitly for
prompt, response, quote, snapshot, evidence, and claim-content leakage.

* Rollback
Rollback must never re-enable Python Delta generation.

Before the table swap, the raw table remains authoritative and no rollback is
needed. After swap, if canonical reads/writes fail:
1. pause Scheduler;
2. preserve the failed v2 table under a timestamped name;
3. rename =delta_audit_log_20260826= back to =deltas=;
4. deploy the prior known-good semantic image if required;
5. keep =TYCHO_DIFFER_MODE=semantic=;
6. verify v2-backed migrated claims still resolve because the audit log contains
   both old rows and all appended v2 replacements;
7. resume only after read-back validation.

Never delete either table or restore =legacy= mode.

* Evidence and handoff updates
Update:
- =docs/tycho-spec.org=
- =docs/architecture.md=
- =docs/semantic-delta-deployment-evidence.org=
- =handoff.org=
- =data/semantic_delta_production.json=

Create bounded machine evidence for:
- legacy-to-v2 mapping;
- replay import/report hashes;
- four missing-pair Gemini runs and cost;
- archive/canonical table inventories;
- claim migration actions;
- final invariant queries;
- scheduler and Runtime route read-backs.

Do not store raw prompts, model responses, evidence quotes, source snapshots, or
claim text in ordinary logs/traces. The existing local replay reports remain the
review artifact and must not be uploaded to Firestore.

* Definition of done
This task is not complete until all are true:
1. Every historical and live canonical Delta is Gemini-generated =delta@2=.
2. =tycho.deltas= contains no =delta@1= or Python-generated row.
3. All 46 legacy rows remain intact in the immutable audit archive.
4. Every legacy pair has exactly one grounded canonical replacement.
5. Production has no mechanical Delta fallback.
6. Backfill caused no Pub/Sub/Runtime/alert traffic.
7. No active claim cites legacy evidence; all active evidence resolves to v2.
8. Scheduler, Runtime dispatcher route, and rollback resources remain healthy.
9. Full tests and direct cloud invariants pass.
10. Exact rollback is documented and does not re-enable Python Deltas.

Return a concise report with counts, IDs of any failed/unresolved pair or claim,
Gemini calls/tokens/cost, table names, claim migration totals, test result,
production read-back, and evidence-file paths. Do not claim completion with any
legacy canonical Delta or active legacy-backed claim remaining.
