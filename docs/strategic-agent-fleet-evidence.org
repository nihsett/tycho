#+TITLE: Tycho Strategic Agent Fleet — Implementation and Production Evidence
#+DATE: 2026-08-26
#+STARTUP: overview

* How to read this document
Two records, in the order they happened:
1. *Local implementation* — the council, its schemas, gates, and offline tests.
2. *Production deployment* — the Runtime, dispatcher, Scheduler, one real
   session, the duplicate trigger, and the telemetry inspection. See
   =Production deployment (2026-08-26)= at the end.

* Scope of the local record
This section records the *local* implementation of the strategy council, built
under the split start gate in =docs/strategic-agent-fleet-handoff.org=.

What this run did:
- built the three named ADK agents, strict schemas, deterministic evidence
  rules, the bounded context builder, and local session/lease persistence;
- ran one synthetic end-to-end session producing one accepted and one rejected
  conclusion;
- ran the full repository gate.

What this run deliberately did NOT do:
- no Gemini or any other model call;
- no read or write against production GCP (BigQuery, Firestore, GCS, Pub/Sub,
  Scheduler, Agent Runtime, Registry, IAM);
- no Agent Runtime deployment and no Registry entry;
- no dashboard;
- no change to =data/tycho.sqlite3=, the main worktree, or the =tycho-analyst-push=
  route.

Production deployment and live strategy runs remain blocked behind the closed
production gate: the physical BigQuery rename and the final post-swap
read-backs. Nothing in this document claims otherwise.

* What was built
** Contracts
- =schemas/strategy.py= — =PremiseReference=, =StrategyCard=, =ChallengeResult=,
  =StrategySession=, =ManifestEntry=, =MetricEvidence=, =SessionMetrics=, plus
  the model-facing draft schemas =StrategyCardDraft=, =StrategyProposal=, and
  =BriefDraft=. All IDs are ULID-shaped and pattern-bounded, all free text has an
  explicit maximum length, and every model rejects unknown fields.
- =schemas/brief.py= — =Brief= gains =strategy_session_id= and
  =strategy_card_ids=. Analyst-era briefs leave both unset and still validate.

The draft schemas the agents emit carry *no* Delta IDs, entities, scopes, or
counts. Those are recomputed in Python, which is what makes handoff rule 10
structural rather than a prompt instruction.

** Deterministic rules
- =pipeline/strategy_evidence.py= — premise resolution against the store,
  canonical-v2 enforcement, source-family normalization, conclusion-language
  policy, staleness, and the confidence ceiling.
- =pipeline/strategy_context.py= — the bounded context manifest, deterministic
  selection, period metrics carrying their contributing Delta IDs, and the
  byte/token budget.
- =pipeline/strategy_lease.py= — the =(period_from, period_to, strategy_version)=
  lease identity shared by both backends.

** Source-family normalization
One entity's own publication channels collapse to one family:
=github_releases=, =website_changelog=, and =blog_rss= all map to
=<entity>/official_release=; =website_pricing= and =x_account= map to their own
first-party families; anything else is =<entity>/independent:<source>=.

On top of that, two Deltas whose grounded quotes are byte-identical after
whitespace/case normalization collapse into one witness *even across families*.
That second rule is what makes republished official text unable to masquerade as
independent corroboration — the exact =cross_source_mirrored_evidence= pattern
the read-only candidate audit found 13 times for =claude_code= between
=github_releases= and =website_changelog=.

** Conclusion-language policy
Five families are detected deterministically: removal, causation, intent,
leadership, and future action. In v1 only *removal* can be evidenced, and only by
a =deprecation= change in a premise Delta. Causation, intent, leadership, and
prediction are rejected outright, because Tycho's evidence cannot support them.

** Agents
| Agent | Name | Output schema | Tools |
|-------+------+---------------+-------|
| Strategist | =tycho_strategist= | =StrategyProposal= | none |
| Challenger | =tycho_challenger= | =ChallengeResult= | none |
| Brief Writer | =tycho_brief_writer= | =BriefDraft= | none |

Each is a strict-structured =LlmAgent= with =include_contents="none"= and both
transfer paths disallowed. ADK's task mode attaches only its own internal
finish-task tool; there is no search, storage, fetch, memory, or claim-mutation
tool on any of them.

** Governed Runtime entrypoint
The deployed root agent is =StrategyCouncilAgent=, a deterministic =BaseAgent=
wrapper following the =runtime_agent/agent.py= pattern. It is deliberately *not*
a =SequentialAgent= over the three =LlmAgent= objects: that shape would run them
back to back with no Python between them, which is exactly where every hard
evidence check lives.

The wrapper:
- parses only a bounded =StrategyRequest= (period plus request ID; any extra
  field is rejected, so prompt text cannot reach an agent);
- constructs =CloudBackend= and the config inside the request, holding no live
  client in the deployed object;
- calls =run_strategy_session=, so the three named agents are invoked only
  through the governed workflow;
- returns only session ID, strategy version, state, card counts, brief ID, and a
  skipped flag;
- carries no outer model, no instruction, and no tools.

=strategy_agent/app.py= builds the app in a factory. Nothing runs at import
time: =agentplatform.init= lives inside =build_strategy_app=, so importing the
package contacts no Google Cloud service. The declared identity requests
=roles/datastore.user=, =roles/bigquery.dataViewer=, =roles/bigquery.jobUser=,
and =roles/telemetry.tracesWriter= — the handoff's trace-writer role, not the
broader legacy =roles/cloudtrace.agent=, which is now in the forbidden prefix
list alongside storage, Pub/Sub, and Memory Bank.

** Atomic session start
=begin_strategy_session= acquires the lease and creates the running session in
ONE transaction. Acquiring the lease first and creating the session afterwards
left a window in which a duplicate trigger saw an active lease and no readable
session. A racing request now either wins and creates both, or loses and reads
the winner's running session immediately.

The running session starts deliberately empty: no manifest, no metrics
evidence, zero counters. It exists so the lease and a readable session appear
together, before any context work or agent call. The final context lands in the
completed document at commit time.

** Atomic persistence
Saving the brief, moving the session to its terminal state, and releasing the
lease are ONE store operation: =commit_strategy_session=, implemented with the
same signature as a single SQLite =BEGIN IMMEDIATE= transaction and a single
Firestore transaction.

The commit also requires lease ownership: the lease must exist, be =active=,
and name the committing session. SQLite checks the =UPDATE= row count; Firestore
validates the lease before any write. Without this, a session whose lease
expired and was reclaimed by a newer attempt could still publish its brief.

This closes the crash window that would otherwise leave a stored brief whose
session is still =running= — a state that permanently blocks every retry for
that period. Either all three writes land, or none do.

Brief IDs are also now unique per session: =brf_2026w35-8xr31dpc=. The weekly
period alone is not a safe key, because a retry after a failed session covers
the same week and would collide with the abandoned attempt's write-once brief.
=BriefId= accepts the plain weekly form and an optional session discriminator,
so analyst-era briefs still validate.

** Durable context failures
The placeholder session is created before the context is built, so a context
failure is recorded rather than lost. The failure path marks the placeholder
session failed and releases the lease atomically, with sanitized error metadata
and an empty manifest. The period stays retryable and the attempt stays visible.

** Error redaction
=strategy_agent/errors.py= is the single sanitizer. It reduces any exception to
a stage, a class name, and a curated reason drawn from a fixed table; it never
reads =str(exc)=. That matters because Pydantic's =ValidationError= renders
=input_value=, so a malformed model response, a claim statement, or a grounded
quote can otherwise end up inside a persisted session, lease, or log line.

An unrecognised exception class collapses to a generic reason, so a new
exception type cannot leak text simply by not having been considered yet.

** Model boundary
=strategy_agent/invoker.py= is the only place a strategy agent reaches a model.
=AdkAgentInvoker= runs one ADK turn through Vertex/Agent Platform ADC. It was
*not* exercised in this task. Every test and the synthetic session use
=ScriptedInvoker=, which returns pre-written structured payloads, so the entire
workflow runs with zero provider calls.

** Evidence consistency
=evidence_defect()= is shared by the context builder and the premise resolver,
so a claim can never be admitted to a session on evidence a card would then be
rejected for. Beyond canonical =delta@2=, a premise's evidence must satisfy:
- the Delta is =meaningful= — a noise Delta records that nothing durable
  changed and is audit and cost evidence, never a premise;
- the Delta's entity matches the claim's entity;
- the Delta's source matches the source recorded on the claim's evidence entry.

* Synthetic end-to-end session
Command: =just strategy-session= (report at =data/strategy_local_session.json=,
disposable store at =data/strategy-local/=).

The synthetic market has four canonical =delta@2= rows across three entities,
including one deliberately mirrored pair: =claude_code= publishing identical
sandbox text to =github_releases= and =website_changelog=.

Result:
- session state =completed=; 2 cards proposed, 1 passed, 1 rejected;
- manifest hash =3d02e2826cd47f3c50f5db92958ff1e419e545927d3dd714f0f6aa8016398481=;
- context 5,362 bytes / ~1,341 estimated tokens, well inside the 200,000-byte
  and 50,000-token budget;
- brief =brf_2026w35-<session>=, pinning exactly the two premise claim versions
  of the passed card;
- agent call order: =tycho_strategist=, =tycho_challenger=, =tycho_brief_writer=.

** The accepted conclusion
Spans =claude_code= and =codex= with two distinct source families, confidence
=likely= (ceiling from two confirmed premises), a competing explanation, and an
observable falsifier. The Challenger passed it; Python had already passed it.

** The rejected conclusion
Rejected by deterministic validation *before* the Challenger was consulted, for
four independent reasons:
1. =a cross-entity conclusion needs 2 distinct entities; got 1=
2. =a conclusion needs 2 independent source families; got 1 after mirrored-evidence normalization=
3. =conclusion asserts unsupported causation=
4. =conclusion asserts unsupported leadership=

The Challenger was invoked exactly once, for the surviving card only. The
rejected card remains in the session audit and its non-shared premise never
appears in the brief.

* Test coverage against the handoff's required list
| Required test | Where |
|---------------+-------|
| exact claim-version pinning | =test_strategy_evidence.py::test_exact_claim_version_pinning_is_required= |
| stale premise handling | =::test_stale_premise_must_be_labelled_and_speculative= |
| canonical-v2 evidence only | =::test_premise_citing_archived_legacy_evidence_is_rejected=, =::test_canonical_store_refuses_to_write_a_legacy_delta= |
| source-family deduplication | =::test_mirrored_release_text_is_one_source_family=, =::test_identical_text_across_unrelated_sources_still_collapses= |
| minimum entity/source diversity | =::test_minimum_entity_and_source_diversity= |
| intent/causation/leadership rejection | =::test_intent_causation_and_leadership_conclusions_are_rejected=, =::test_removal_is_allowed_only_with_deprecation_evidence= |
| zero-card legitimate outcome | =test_strategy_council.py::test_zero_cards_is_a_valid_result_and_stores_an_empty_brief= |
| Challenger cannot override hard failure | =::test_challenger_pass_cannot_override_a_hard_failure= |
| rejected card excluded from brief | =::test_rejected_card_is_visible_in_the_audit_but_absent_from_the_brief= |
| citation marker validation | =test_strategy_citations.py= (9 tests), =test_strategy_council.py::test_brief_citing_an_unpinned_claim_fails_the_run= |
| reproducible brief after supersession | =::test_brief_stays_reproducible_after_a_later_supersession= |
| duplicate trigger lease | =test_strategy_persistence.py::test_duplicate_trigger_returns_the_existing_session_without_a_model_call= |
| failed retry | =::test_a_failed_session_stays_retryable=, =::test_an_expired_lease_is_reclaimed= |
| local/cloud parity | =::test_local_and_cloud_strategy_surfaces_match= (14 methods), =::test_both_backends_satisfy_the_strategy_session_protocol= |
| input budget failure before Gemini | =test_strategy_local_run.py::test_input_budget_failure_happens_before_any_agent_call= |
| redacted traces/events | =test_strategy_agents.py::test_the_strategy_app_exports_redacted_traces_only=, =::test_an_event_containing_content_is_refused=, =test_strategy_council.py::test_events_are_redacted_and_name_the_agents= |
| no GCS/web-search tool or permission | =::test_no_agent_has_a_web_search_or_storage_tool=, =::test_runtime_identity_requests_no_storage_pubsub_or_memory_bank= |
| no claim mutation during a session | =test_strategy_council.py::test_session_never_mutates_claims_or_publishes=, =test_strategy_evidence.py::test_no_claim_mutation_during_a_normal_validation_pass= |

Review-blocker coverage added after the first draft:

| Blocker | Where |
|---------+-------|
| governed Runtime entrypoint, not a raw sequence | =test_strategy_agents.py::test_the_runtime_root_is_a_deterministic_wrapper_not_a_raw_sequence=, =::test_the_runtime_root_holds_no_model_and_no_tools= |
| Runtime returns bounded IDs/state/counts only | =::test_the_runtime_returns_only_bounded_ids_state_and_counts= |
| Runtime rejects anything but a bounded request | =::test_the_runtime_rejects_anything_but_a_bounded_request= |
| trace-writer role, not the Cloud Trace agent role | =::test_runtime_identity_uses_the_handoff_trace_writer_role= |
| atomic brief + session + lease commit | =test_strategy_recovery.py::test_a_failed_commit_writes_no_brief_session_state_or_lease_change=, =::test_a_brief_collision_rolls_back_the_whole_commit= |
| a crash never permanently blocks retries | =::test_a_crash_before_the_commit_leaves_the_period_retryable= |
| a retry creates a unique brief | =::test_a_retry_creates_a_unique_brief_for_its_new_session= |
| a duplicate trigger finds a readable running session | =::test_a_concurrent_duplicate_trigger_finds_a_readable_running_session= |
| commit refuses a non-terminal or already-terminal session | =::test_commit_refuses_a_running_terminal_state= |
| a secret in model output never reaches storage | =::test_a_pydantic_error_carrying_model_output_is_never_persisted= |
| the sanitizer never reads the exception message | =::test_the_sanitizer_never_reads_the_exception_message=, =::test_sanitized_text_is_bounded_and_structured=, =::test_events_never_carry_error_text= |
| noise Deltas are not premises | =test_strategy_evidence.py::test_a_noise_delta_is_not_a_premise= |
| Delta entity/source must match the claim | =::test_a_premise_whose_delta_entity_differs_is_rejected=, =::test_a_premise_whose_delta_source_differs_is_rejected= |
| the context builder applies the same rule | =::test_the_context_builder_applies_the_same_evidence_rule= |

Final persistence fixes:

| Issue | Where |
|-------+-------|
| no lease/session race | =test_strategy_recovery.py::test_a_duplicate_trigger_between_lease_and_session_reads_the_running_session= (a real second trigger on its own connection and event loop, fired after acquisition and before context or any agent) |
| begin is atomic both ways | =::test_begin_is_atomic_no_lease_without_a_session=, =::test_begin_rolls_back_the_lease_when_the_session_insert_fails=, =::test_begin_refuses_a_non_running_session= |
| commit requires an existing lease | =::test_commit_refuses_when_the_lease_is_missing= |
| commit requires lease ownership | =::test_commit_refuses_a_lease_owned_by_another_session= |
| commit requires an active lease | =::test_commit_refuses_an_already_released_lease= |
| context failure stays visible and retryable | =::test_a_context_failure_marks_the_placeholder_failed_and_retryable=, =test_strategy_local_run.py::test_input_budget_failure_happens_before_any_agent_call= |
| lease + session creation is a parity method | =test_strategy_persistence.py::test_local_and_cloud_strategy_surfaces_match= |

* Repository gate
=just check= (lock check, Ruff, compileall, pytest): *249 passed*, up from the
92-test baseline. 157 of those are new strategy tests.

* just recipes added
| Recipe | Effect |
|--------+--------|
| =strategy-session= | one synthetic offline session end to end |
| =strategy-brief= | the same, plus the rendered brief markdown |
| =strategy-test= | only the strategy suites |
| =strategy-stats= | sessions and briefs in the disposable store |
| =strategy-clean= | delete the disposable store and its report |

All five are synthetic and offline. =strategy-session= refuses to run against
=data/tycho.sqlite3=.

* Production deployment (2026-08-26)
The production gate opened when the strict BigQuery swap completed. Everything
below was verified directly against =gen-lang-client-0110801105= /
=us-central1=; nothing is inferred from a previous document.

** Production gate, re-verified read-only before deploying
| Gate | Observed |
|------+----------|
| canonical =tycho.deltas= | 50 rows, all =delta@2=, 0 noncanonical provenance, 13 meaningful / 37 noise, 50 distinct Delta and comparison IDs |
| canonical inventory hash | =d0b4b08fe59dbc3ad45e0e9668a0eea9a342a39d6d00eaa2f7e2001421ff90bf= |
| audit =tycho.delta_audit_log_20260826= | 96 rows, 46 legacy + 50 v2, hash =e422901acc4fef75c0fa1ca94fbf112ddc008c86ce89c377e0774c55a5c8f4bc= |
| =tycho.deltas_v2_candidate= | absent |
| observations | 92 |
| claims | 38 documents: 11 active, 27 retired |
| active-claim evidence | all 11 resolve only to canonical meaningful =delta@2=; 0 evidence defects under =evidence_defect()= |
| acquisition job | generation =7=, =TYCHO_DIFFER_MODE=semantic=, digest =sha256:e993e3df84f047da3fdee4fcbb43d543e97a09d559f0157fac9426b74169cbf3= |
| =tycho-nightly= | =ENABLED=, =0 2 * * *= =Etc/UTC=, no acquisition execution active |
| =tycho-analyst-push= | endpoint/audience =https://tycho-analyst-dispatcher-u2s544lf5a-uc.a.run.app=, OIDC =tycho-agent-dispatcher@…=, ack deadline 600s, backlog 0 across 29 consecutive samples |
| Analyst Runtime | =…/reasoningEngines/8577815225082839040= present, =AGENT_IDENTITY=, min 0 / max 1; its Cloud Run dispatcher =Ready=True= |
| repository | branch clean, =just check= = 249 passed |

The three active entities are =claude_code=, =codex=, and =pi=; each contributes
exactly one source family (=<entity>/official_release=).

** Resources created
| Resource | Identity |
|----------+----------|
| Agent Runtime | =projects/548847028907/locations/us-central1/reasoningEngines/1864848136766226432= (=Tycho Strategy Council=) |
| Registry entry (automatic) | =projects/gen-lang-client-0110801105/locations/us-central1/agents/agentregistry-00000000-0000-0000-cc6a-d57077c7d164= |
| Registry agent ID | =urn:agent:projects-548847028907:projects:548847028907:locations:us-central1:aiplatform:reasoningEngines:1864848136766226432= |
| Managed Agent Identity | =agents.global.proj-548847028907.system.id.goog/resources/aiplatform/projects/548847028907/locations/us-central1/reasoningEngines/1864848136766226432= |
| Cloud Run dispatcher | =tycho-strategy-dispatcher= at =https://tycho-strategy-dispatcher-u2s544lf5a-uc.a.run.app= |
| Dispatcher service account | =tycho-strategy-dispatcher@gen-lang-client-0110801105.iam.gserviceaccount.com= |
| Scheduler job | =projects/gen-lang-client-0110801105/locations/us-central1/jobs/tycho-strategy-weekly= |
| Staging bucket | =gen-lang-client-0110801105-tycho-agent-staging= (pre-existing, reused) |

Runtime: =createTime 2026-08-26T16:14:48Z=, =updateTime 2026-08-26T16:56:49Z=,
=identityType AGENT_IDENTITY=, =agent_framework google-adk=, min =0= / max =1=,
labels =app=tycho, environment=production, version=tycho-strategy-council-1=.

*** Exact IAM
Runtime managed identity holds *exactly* four project roles, read back from the
live policy and compared as a set:
- =roles/bigquery.dataViewer=
- =roles/bigquery.jobUser=
- =roles/datastore.user=
- =roles/telemetry.tracesWriter=

Zero roles matching the forbidden prefixes =roles/storage=, =roles/pubsub=,
=roles/aiplatform.memoryBank=, or =roles/cloudtrace=. No editor, owner, or
=aiplatform.admin=. The readback fails closed on any wider *or* narrower set.

The identity additionally inherits the project's pre-existing
=roles/aiplatform.agentDefaultAccess= binding on
=principalSet://agents.global.proj-548847028907.system.id.goog/attribute.platformContainer/aiplatform/projects/548847028907=,
which is the platform default applied to every Agent Identity in the project
(the Analyst Runtime and the disposable probe carry it too). It was not granted
by this deployment.

Dispatcher service account holds exactly =roles/aiplatform.viewer= (which
includes =reasoningEngines.query=) and =roles/logging.logWriter=, plus
=roles/run.invoker= on its *own* Cloud Run service so its Scheduler job can call
it. It has no data-store, GCS, or Pub/Sub access.

*** Dispatcher and Scheduler read-backs
Dispatcher: =--no-allow-unauthenticated=, ingress =all=, runs as
=tycho-strategy-dispatcher@…=, and the only =roles/run.invoker= member on the
service is that same identity — no =allUsers= or =allAuthenticatedUsers=. The
readback treats a public binding as a hard failure.

Scheduler =tycho-strategy-weekly=: schedule =0 6 * * 1=, time zone =Etc/UTC=,
state =ENABLED=, method =POST=, URI and OIDC audience both the dispatcher URL,
OIDC identity =tycho-strategy-dispatcher@…=, attempt deadline =1800s=, next run
=2026-08-31T06:00:00Z=. Its static body is
={"trigger":"scheduler","period":"previous_complete_week"}= — a *name*, never a
date range, so the job cannot widen the window and the dispatcher resolves the
week from its own clock.

** The one production session
Triggered by =gcloud scheduler jobs run tycho-strategy-weekly= at
=2026-08-26T17:04:39Z= — the real job, real OIDC, real endpoint. Dispatcher
returned HTTP =200= in =24.101s=.

| Field | Value |
|-------+-------|
| request ID | =scheduler-2026w34= |
| period | =2026-08-17T00:00:00Z= → =2026-08-24T00:00:00Z= (ISO week 2026-W34) |
| session | =sts_01M0ZGG793KQK3BTCVPAP3DH2D=, state =completed= |
| brief | =brf_2026w35-pap3dh2d= |
| manifest | 11 pinned =(claim_id, version)= entries |
| manifest hash | =eaff310db5b2964af12c2251cbbc0cbb9654bcdeec447ab8bb5b7b629ad76589= (independently recomputed from the stored manifest and identical) |
| context | 22,866 bytes / ~5,717 estimated tokens, inside the 200,000-byte and 50,000-token budget |
| cards | 2 proposed, *0 passed*, 2 rejected |
| challenges | 0 — both cards failed deterministic validation before the Challenger was consulted |
| run IDs | =sts_01M0ZGG793KQK3BTCVPAP3DH2D:strategist= |
| model | =gemini-3.5-flash-lite= for all three agent slots |
| tokens | 8,047 in / 485 out / 8,532 total |
| latency | 3,137 ms of model time |
| cost | ≤ =$0.0079= (priced at the Gemini 3.7 Flash promotional rate as a documented upper bound; Flash-Lite is cheaper) |

*** Why zero cards is the honest answer
Both drafts were rejected by =pipeline/strategy_evidence.py=, by
Python-generated reasons only:

| Card | Deterministic rejection reasons |
|------+---------------------------------|
| =stc_01M0ZGGMZ5GF44D2R8M7FT4GYG= (=claude_code=, =codex=; 2 source families) | conclusion asserts unsupported causation |
| =stc_01M0ZGGPA4W87FCF28W9RBCS50= (=claude_code= only; 1 source family) | needs 2 distinct entities, got 1; needs 2 independent source families, got 1 after mirrored-evidence normalization; conclusion asserts unsupported causation |

The first card cleared entity and source-family diversity and still failed on
the conclusion-language policy. Nothing was relaxed to produce a result, and no
conclusion was manufactured: the session stored the deterministic empty brief
(=482= bytes, zero citations, zero pinned claims, no URL) stating that no
defensible cross-entity pattern survived validation. That is a valid outcome
under the spec.

Brief statistics recomputed in Python for the period: =new=5=, =superseded=0=,
=confidence_changes=0=, =stale_flagged=0=.

*** Two earlier failed attempts, and the defect they found
The first trigger at =2026-08-26T16:48:54Z= failed, and Cloud Scheduler's own
single retry at =16:50:03Z= failed the same way. Both are recorded honestly:

- dispatcher returned HTTP =503= both times, so the period stayed retryable;
- sessions =sts_01M0ZFMGKMR4E82D5XA37D3DG9= and
  =sts_01M0ZFN5N3WDNQT77Z9K81BZAE= are durable =failed= records;
- the lease reached =attempt=2=, =state=failed=, and stayed reclaimable;
- both stored errors are the sanitized
  =strategist:StrategyModelError: agent returned no usable structured output= —
  no model output, claim text, or quote reached storage;
- zero briefs were written, zero claims mutated.

Root cause: =strategy_agent/invoker.py= read only the final *text* response.
ADK's =mode="task"= agents do not answer with text — a strict-structured task
agent delivers its output as the arguments of one =finish_task= function call,
and the final event carries only the tool's confirmation string. The invoker had
never been exercised against a real provider before this run. It now reads the
last =finish_task= call's arguments and falls back to JSON text, covered by five
regression tests. The Runtime package was updated at =2026-08-26T16:56:49Z= and
the next trigger completed.

The failure path is therefore not a caveat on this deployment; it is the part of
it with the most direct evidence.

** Duplicate trigger
The same Scheduler job was fired a second time at =2026-08-26T17:05:36Z=,
normalizing to the identical =(period_from, period_to, strategy_version)= lease
identity.

| Check | Result |
|-------+--------|
| dispatcher response | HTTP =200= in =2.665s= (versus =24.101s= for the real run) |
| =skipped= | =true= |
| session returned | =sts_01M0ZGG793KQK3BTCVPAP3DH2D= — the existing one |
| brief returned | =brf_2026w35-pap3dh2d= — the existing one |
| counts returned | identical: 2 proposed / 0 passed / 2 rejected |
| completed sessions for the period | exactly =1= |
| briefs for the period | exactly =1= |
| model calls across every session for the period | =1= |
| Runtime trace for the duplicate | =15c20693e82c277a9d28f9bc60929053=, 2 spans, *zero* =generate_content= spans |

The trace is the decisive part: the duplicate's Runtime invocation produced only
=invoke_workflow= and =invoke_agent= spans for =tycho_strategy_council=. No
=call_llm=, no =generate_content=, no =tycho_strategist=.

** Telemetry inspection
Every persisted trace in the window from =2026-08-26T16:34:51Z= was fetched and
inspected directly — =8= traces, =4= Runtime invocations plus =4= Cloud Run
dispatcher request traces — together with =15= dispatcher log entries.

| Trace | Start | Spans | =generate_content= | What it is |
|-------+-------+-------+-------------------+------------|
| =811df7f93f807bdad1ad89d35e746345= | 16:48:59Z | 3 | 0 | dispatcher request, attempt 1 |
| =d36c815819d58c78b7077bfd984d00d1= | 16:49:41Z | 7 | 1 | Runtime, attempt 1 (failed) |
| =d9871a310d19f96896ed4ff5f4d21d77= | 16:50:03Z | 3 | 0 | dispatcher request, Scheduler retry |
| =35f57753c87ce492960ddbe0e07c430c= | 16:50:04Z | 13 | 3 | Runtime, attempt 2 (failed; ADK schema retries) |
| =ad1477437e3ab158588cbcc472e24988= | 17:04:44Z | 3 | 0 | dispatcher request, the production session |
| =e6c084c74e5fadadf72da735f3ba47b8= | 17:04:47Z | 7 | 1 | *Runtime, the production session* |
| =4601a3a7c8327cc80226caf40e8ad3c8= | 17:05:39Z | 3 | 0 | dispatcher request, duplicate trigger |
| =15c20693e82c277a9d28f9bc60929053= | 17:05:40Z | 2 | 0 | *Runtime, duplicate trigger — no model call* |

Runtime span names are structural only: =invoke_workflow tycho_strategy_council=,
=invoke_agent tycho_strategy_council=, =invoke_workflow tycho_strategist=,
=invoke_agent tycho_strategist=, =call_llm=,
=generate_content gemini-3.5-flash-lite=, =execute_tool finish_task=. Span
attributes are limited to =gen_ai.operation.name=, =gen_ai.agent.name=,
=gen_ai.agent.description=, =gen_ai.request.model=,
=gen_ai.response.finish_reasons=, =gen_ai.usage.input_tokens=,
=gen_ai.usage.output_tokens=, =gcp.vertex.agent.invocation_id=,
=gcp.vertex.agent.event_id=, and platform/SDK resource labels.

*** What was explicitly searched for, and not found
Two independent checks, both clean:

1. *Forbidden field names* — =0= hits across all 8 traces and all 15 log
   entries for: =llm_request=, =llm_response=, =system_instruction(s)=,
   =user_messages=, =tool_definitions=, =gen_ai.prompt=, =gen_ai.completion=,
   =gen_ai.response.text=, =message_content=, =input_value=, =output_value=,
   =input.value=, =output.value=, =prompt=, =completion=, =statement=,
   =rationale=, =quote=, =rendered_md=, =model_output=, =request_content=.
2. *The governed prose itself* — =0= hits. Scanning for field names only catches
   leaks somebody already anticipated, so =infra/verify_strategy_production.py=
   also pulls the real text the session read and wrote out of the store — claim
   statements, claim rationales, Delta change statements, grounded evidence
   quotes, every proposed card's statement/rationale/competing
   explanation/falsifier, and the brief's own prose — and proves none of it
   occurs anywhere in any trace or log payload. =122= normalized snippets were
   searched, case- and whitespace-insensitively. None appeared.

The dispatcher's four bounded result lines carry only: =service=, =request_id=,
=trigger=, =period_from=, =period_to=, =strategy_version=, =runtime=,
=session_id=, =state=, =cards_proposed=, =cards_passed=, =cards_rejected=,
=brief_id=, =skipped=, and the sanitized =error=. That set is enforced by an
allowlist, so a content-bearing field cannot appear by being added upstream.

** Existing production was not touched
Snapshots taken before deployment (=16:14:02Z=) and after the session and
duplicate trigger (=17:20Z=) are byte-identical for every unrelated resource:

| Resource | Before | After |
|----------+--------+-------|
| =tycho-analyst-push= endpoint, audience, OIDC identity, ack deadline, topic | unchanged | unchanged |
| =tycho-nightly= schedule/time zone/state | =0 2 * * *=, =Etc/UTC=, =ENABLED= | identical |
| =tycho-acquire= generation / image digest / differ mode | =7= / =sha256:e993e3df…= / =semantic= | identical |
| BigQuery canonical rows / v2 rows / audit rows / observations | 50 / 50 / 96 / 92 | identical |
| canonical Delta inventory hash | =d0b4b08f…= | identical |
| Firestore claim documents / active / retired | 38 / 11 / 27 | identical |
| Firestore =analyst_runs= | 57 | identical |
| Firestore =alerts= | 0 | identical |

The only differences anywhere are the new strategy resources: Cloud Run gains
=tycho-strategy-dispatcher=, the Runtime list gains =Tycho Strategy Council=,
and Firestore gains =strategy_sessions= 0 → 3, =strategy_leases= 0 → 1, and
=briefs= 0 → 1. No Pub/Sub message was published, no Analyst Runtime invocation
occurred, no claim, Delta, or Observation was written.

** What was built for this deployment
| Module | Role |
|--------+------|
| =pipeline/strategy_dispatcher.py= | the private authenticated dispatcher: derives the week, normalizes the trigger into the existing bounded =StrategyRequest=, parses a bounded Runtime result, logs structure only |
| =infra/deploy_strategy_council.py= | resumable, idempotent deployment across nine steps; structurally incapable of modifying the analyst path |
| =infra/verify_strategy_production.py= | read-only session, telemetry, and duplicate-trigger verification |
| =data/strategy_council_production.json= | deployment state, read-backs, and the before/after production snapshots |
| =data/strategy_production_session.json= | session, telemetry, and duplicate-trigger evidence |

*** The deployment tool cannot touch =tycho-analyst-push=
Every shell-out goes through one =run()= wrapper that calls =guard_command()=
first. A command naming =tycho-analyst-push=, =tycho-analyst-dispatcher=,
=tycho-analyst=, =tycho-nightly=, =tycho-acquire=, =tycho-deltas=,
=tycho-agent-dispatcher=, or =tycho-runtime= is refused unless it is
unambiguously read-only (=describe=, =list=, =get-iam-policy=, =read=, =tail=).
The string =modify-push-config= does not occur in the module at all. Nine
parametrized tests assert the refusals, four assert that reads still work for
evidence, and one asserts the strategy resources are not caught by the guard.

Pointing the tooling at the Analyst Runtime by resource name is separately
refused: =readback_runtime= raises =ProtectedResourceError= on the display name
=Tycho Analyst= before anything is written, and =update= runs that readback
before it reaches the API at all.

*** Resume and idempotency, proved in production
The first deployment run was killed mid-Scheduler-step. On re-run with
=--resume= the tool read all eight completed resources back from the API,
created nothing a second time, and applied only the missing Scheduler step —
exactly the designed behaviour, observed against the live project rather than a
fixture. The =untouched_before= snapshot was captured once and never
overwritten.

Offline, the same properties are covered by: a second deploy applying nothing
and reading everything back; a mid-way failure leaving earlier steps recorded
and only the missing ones applied on resume; a second deploy without =--resume=
refusing rather than redeploying; an orphaned same-name Runtime blocking a
second create; a state file for another project failing closed; and readback
refusing a deployment that never finished.

** Defects found and fixed during this deployment
1. *The model boundary never read ADK task-mode output.* Described above. Fixed
   in =strategy_agent/invoker.py=; five regression tests.
2. *Cloud Trace pagination silently truncated the inspection.* Cloud Trace v1's
   =pageSize= is a scan budget over the project's traces, not a result count: a
   single page returned 1 of the 8 matching traces at =pageSize=100= and 0 at
   =pageSize=20=. Claiming "every persisted trace was inspected" would have been
   false. =fetch_traces= now follows =nextPageToken= to exhaustion, de-duplicates
   by trace ID, and raises rather than truncate if paging does not terminate.
   Four regression tests.
3. *The brief was named after the wrong ISO week.* =period.to= is exclusive, so
   a Monday-aligned weekly window ends at the first instant of the *following*
   week; naming the brief after that instant labelled week 34's report as week
   35. =period_week()= now uses the last moment the period actually covers. Two
   regression tests, and the pre-existing mid-week synthetic case is unchanged.
   *The one stored production brief predates this fix*: =brf_2026w35-pap3dh2d=
   covers ISO week 2026-W34. Briefs are write-once and it was not rewritten.
4. *=gcloud scheduler jobs update http= rejects =--headers=.* It takes
   =--update-headers= for the same map, so the idempotent re-apply path failed
   on its first real use. Fixed per verb.
5. *Cloud Scheduler normalizes the target URI with a trailing slash.* The
   readback failed closed on =https://host/= versus =https://host=. Endpoints are
   now compared modulo one trailing slash; a genuinely different host is still
   drift. Six regression tests.

** Test coverage added
| Suite | Tests | Covers |
|-------+-------+--------|
| =tests/test_strategy_dispatcher.py= | 34 | weekly period derivation, duplicate triggers sharing one lease identity, unknown-field rejection by name, malformed/oversized bodies, authentication, the exact payload sent to the Runtime, bounded result parsing, a content-bearing result being refused, structural-only logs, and the dispatcher reaching no store, topic, or snapshot |
| =tests/test_strategy_deploy.py= | 52 | the analyst-path guard (refusals and permitted reads), the exact Runtime IAM allowlist and forbidden roles, IAM readback failing closed both ways, the dispatcher identity's narrow roles, scale-to-zero Agent Identity config, a public dispatcher and a drifted Scheduler job as hard failures, endpoint comparison, resume/idempotency, and structural-only state |
| =tests/test_strategy_production_checks.py= | 28 | forbidden field-name detection, governed-prose leak detection under case/whitespace changes, premise re-resolution against the store, the zero-card result, cost labelling, trace pagination, and dispatcher log-line parsing |
| =tests/test_strategy_agents.py= | +5 | ADK task-mode =finish_task= output extraction, rejected-draft precedence, wrapped non-object schemas, and other tool calls not being mistaken for output |
| =tests/test_strategy_council.py= | +2 | the brief being named after the ISO week it actually covers |

=just check= (lock check, Ruff, compileall, pytest): *370 passed*, up from 249.
The strategy suites are now *278* tests, up from 157.

** Commands
#+begin_src bash
just strategy-plan        # print the deployment plan; contacts nothing
just strategy-deploy      # PRODUCTION: resumable, idempotent deploy (asks to confirm)
just strategy-readback    # read every deployed resource back
just strategy-snapshot    # record the untouched acquisition/Analyst state
just strategy-verify      # re-verify the latest production session
just strategy-telemetry   # re-inspect traces and dispatcher logs for leakage
#+end_src

Only =strategy-deploy= writes. It requires an interactive =yes= and fails closed
without a terminal.

** Still open
- The dashboard is deliberately not built; this task stops at the backend and
  its deployment evidence.
- The first natural *meaningful* =delta@2= end-to-end Analyst validation remains
  a separate pending gate, unchanged by this work.
- No strategy card has yet passed in production. With three entities publishing
  through one source family each, a card must clear both diversity rules and the
  conclusion-language policy; the next session runs on Monday
  =2026-08-31T06:00:00Z=.
- =GOOGLE_API_KEY= in the local =.env= still needs rotation before sharing.
